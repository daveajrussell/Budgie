import { OutgoingTypeFilterPipe } from './outgoing-type-filter.pipe';

describe('OutgoingTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OutgoingTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
